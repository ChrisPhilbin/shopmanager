<% title "Add a new car" %>

<%= form_for @car do |f| %>

	<%= f.label_for "Year" %>
	<%= f.text_area :year %>

	<%= f.label_for "Make" %>
	<%= f.text_area :make %>

	<%= f.label_for "Model" %>
	<%= f.text_area :model %>

	<%= f.submit "Create!" %>

<% end %>